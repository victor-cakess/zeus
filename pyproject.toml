[project]
name = "zeus"
version = "0.1.0"
description = "Energy project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "boto3>=1.43.6",
    "pre-commit>=4.6.0",
    "pyarrow>=24.0.0",
    "requests>=2.32.3",
    "ruff>=0.15.12",
]
